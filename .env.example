# Optional. Blank keeps injected-wallet connections available.
VITE_WALLETCONNECT_PROJECT_ID=
# Browser configuration only. Never put a private key in a VITE_ variable.
# Existing testnet browsing and local deployment require no private-key file.
