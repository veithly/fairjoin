# 安全与信任边界

FAIRJOIN 当前是 Monad Testnet / 本地 EVM 的实验性产品，没有经过独立安全审计，不用于保管真实资金。

## 资产和权限

业务合约在部署时固定一个六位精度 ERC-20 资产。FJUSD 是无价值测试币，不是美元、Circle USDC、收益凭证或供应商会员权益。TestUSDC 仅用于本地失败与回滚测试。

成员付款进入合约，由合约规则限制领取和提取；这不等于资金始终留在原钱包。组织者可在已成团且报名截止后收取约定本金，不需要等待活动开始或履约确认。合约没有争议仲裁、服务担保、管理员任意提款或升级接口。

转账前后余额核验会拒绝某些非标准代币行为，不能据此宣称兼容所有 ERC-20。直接转币不会自动报名；误转资产没有通用找回保证。

## 测试和部署

`npm test` 只执行本地 EVM 或明确标记的隔离夹具，不广播测试网或主网交易。`npm run deploy:local` 固定使用本机 31337 开发链的解锁测试账户。

GitHub Actions 不部署产品、不签名，也不需要私钥。客户端可以用公开测试网配置直接启动；WalletConnect 的项目标识是可选的前端配置。

不要提交 `.env`、助记词、私钥、供应商 API Token、钱包存储、浏览器资料、含秘密的日志或网络捕获。不要把任何秘密放入 `VITE_` 变量。仓库检查只能识别列出的常见风险模式，不是完整的秘密检测或安全审计。

## 报告潜在漏洞

涉及资金权限、私钥、凭据或可利用漏洞时，请使用仓库 Security 页的私密漏洞报告入口，不要先在公开 Issue 中贴出利用细节或敏感数据。未开放私密报告入口时，先通过维护者公开资料提供的联络方式建立私密渠道；不要自行发布秘密。

报告应包含受影响提交、最小复现、预期与实际行为、影响范围，以及可用的脱敏回执。请仅使用自己控制的本地或无价值测试资产，不攻击其他人的活动，不尝试实际资金盗取。

## 数据与服务边界

链上地址、公开活动描述、交易与金额可被他人读取。不要把密码、联系方式、内部预算备注等不应公开的信息写入活动描述。

项目的交互记录默认关闭，开启后仅存本地；钱包、RPC 和静态托管服务仍可能观察请求。真实用户使用、主网发布及资产适配前，需要进一步的安全、隐私和合规评估。
