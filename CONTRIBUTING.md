# 参与开发

欢迎围绕同价分摊、交易恢复和消费者体验提出改进。提交应当说明用户价值，并附上与改动相关的验证；不要用功能数量替代完整的资金闭环。

## 启动与验证

使用 `.nvmrc` 指定的 Node.js 22（至少 22.12）。运行 `npm ci`，然后 `npm run dev`。默认连接已公开的 Monad 测试网部署，不需要任何项目私钥。

提交前执行：

```bash
npm run check:repo
npm test
npm run build
git diff --check
git diff -- src/generated/abi.ts
```

仓库检查读取 Git 索引，因此新增文件需要先按白名单暂存。合约变更通过 `npm run contracts:compile` 更新 ABI；检查源码与生成结果是否一起提交。不能把本地测试结果称为真实测试网验证。

## 仓库边界

源码仓库只保留运行所需前端、合约、Worker、配置、精简公开证据以及七个可复用工程脚本。`scripts/` 不是临时工作区；新增入口需要同时修改白名单和内容检查，并解释为什么应长期维护。

不要上传截图、录制视频、配音、PPT、PDF、浏览器资料、研究对话、代理指令、临时诊断脚本、构建目录、依赖副本或秘密。媒体请用独立托管的链接展示，不通过 Git LFS 绕过这个边界。

`.gitignore` 防止通常的误添加；CI 的 `check:repo` 进一步检查已提交内容。不要使用 `git add -f` 绕过规则。扫描不是完备的，发布前仍需人工检查 `git diff --cached`。

## 资金逻辑变更

需要覆盖资金守恒、余数、名额竞争、旧报价、拒签、取消、失败退款以及转账回滚；资金状态必须以合约和匹配的交易回执为依据。成功上链、业务语义异常、尚未广播和广播结果未知是不同情况，不能合并为同一个绿色成功状态。

不得在没有版本迁移计划时改变已部署合约语义。新功能的文档应区分「当前实现」「实验」「待验证方向」，不得虚构客户、收入、测试覆盖率或审计结论。

## Issue 与 Pull Request

普通缺陷请提供网络、浏览器、最小复现和脱敏错误；Pull Request 应说明修改目的、涉及边界、测试命令及结果。安全漏洞使用 [安全说明](SECURITY.md) 中的私密渠道。

仓库保留各文件已有的版权和许可证标记；本次整理没有额外替全部源代码指定新的许可证。
